const Enum = require('src/domain/enum/Enum');

module.exports = Enum({
    BUSINESS: 'business',
    CONTRACT: 'contract',
    INTEGRATION: 'integration',
    OPERATION: 'operation',
    NOT_FOUND: 'notFound'
});
